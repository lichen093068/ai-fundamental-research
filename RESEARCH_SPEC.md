# AI × Finance 自動化收入 Project
# RESEARCH_SPEC v0.1

**狀態：** Phase 1 實作規格（預註冊草案）  
**版本：** v0.1  
**研究順序：** Phase 1（基本面）完成且鎖定後，才可開始 Phase 2（市場反應）  
**適用資料頻率：** 季度為主；年度為輔  
**規格原則：** 本文件中的「不得」與「必須」是實作約束，不是建議。

---

## 1. 研究問題與可否證假設

### 1.1 核心問題

本專案不以技術指標或短期股價預測為研究目的。Phase 1 檢驗在公開、當時可得的財務資訊下，公司的基本面訊號是否與其**未來基本面表現**有可重現的關聯。

Phase 1 的正式問題為：

> 在公司 `i` 的資訊可得日 `A(i,t)`，以不晚於該日可得的季度資料建立訊號 `S(i,t)`；該訊號是否能解釋或排序公司於未來 `h` 季的基本面目標 `Y(i,t,h)`？

這是條件式關聯與排序能力的研究，不構成因果宣稱、投資建議，亦不預設能預測股價。

### 1.2 主要假設

- `H0`：在控制預先定義的產業與時間效果後，訊號與未來基本面目標沒有系統性關聯；橫斷面排序表現不優於隨機排序。
- `H1`：至少一項預先定義訊號與一項預先定義目標存在方向一致、樣本外可重現的關聯。

研究不預設每一訊號必然為正向。若資料、經濟意義或產業特性支持反向關係，必須在進入 validation 前以修訂版預註冊固定方向與理由。

---

## 2. 研究 Universe 與樣本單位

### 2.1 母體

母體為台灣上市與上櫃一般產業公司；每一筆觀測值為一個公司—財報季度 `(i,t)`。

### 2.2 排除範圍

暫時排除下列金融與非一般企業工具：

- 金融保險業、銀行、票券、金控與信用卡等金融機構；
- 證券、期貨、投信投顧及相關金融服務；
- ETF、ETN、基金、REITs 或其他以投資組合／特殊目的工具為核心者；
- 其他財務報表結構不適合一般企業營收、COGS、營業費用與現金流模型的特殊金融商品。

排除與產業分類必須使用**歷史時點有效**的交易所／主管機關分類或可追溯的分類對照表；不得以目前分類回填過去。分類改變時保留生效日與理由。

### 2.3 納入規則

公司在季度 `t` 納入分析，須同時符合：

1. 在 `A(i,t)` 前屬於上述母體且未落入排除範圍；
2. 該季有可驗證的正式財報或法定申報；
3. 計算該訊號與該目標所需欄位皆可由當時可得資料取得；
4. 幣別、期間與合併口徑可識別；
5. 不因為後來下市、破產、併購或停止申報而回溯移除。

新上市公司自第一個具有足夠歷史季度的訊號起納入。下市、停止申報、重整與失敗公司必須保留至最後一個可用季度，並在資料狀態欄記錄原因。

---

## 3. 時間、公告日與 point-in-time 規則

### 3.1 日期欄位

每筆公司—季度至少保存：

| 欄位 | 定義 |
|---|---|
| `fiscal_period_end` | 財報涵蓋期間的結束日 |
| `fiscal_year`、`fiscal_quarter` | 公司所申報的會計年度與季度，不假定所有公司曆年制 |
| `publication_date` | 公司或監管平台首次公開該份文件的日期 |
| `publication_time` | 若可得，首次公開時間與時區；不可得時標示缺失 |
| `availability_date` | 可在一般公開管道取得並可供資料管線使用的最早日期 |
| `as_of_date` | 建立訊號所使用的資訊截點；等於或晚於 `availability_date` |
| `revision_date` | 後續更正／重編版本的公開日；無則為空 |
| `source_document_id` | 原始文件或公告的唯一識別與連結／檔案雜湊 |

### 3.2 資訊可得日

正式訊號日 `A(i,t)` 定義為該季正式財報之 `availability_date`。

- 若有可信時間戳記，公告在交易日收盤後者，訊號最早自下一交易日生效；收盤前者可自同日收盤後的研究截點生效。Phase 1 不使用價格，仍須保存此規則以便 Phase 2。
- 若只有日期、無時間戳記，為保守避免日內資訊錯置，`A(i,t)` 一律視為該公告日結束後，最早下一交易日／下一日可用。
- 月營收、法說會簡報、新聞稿可作輔助欄位，**不得**取代季度正式財報作為本版五大訊號的主資料；若日後啟用，必須另行版本化與預註冊。

### 3.3 更正、重編與後見資料

訊號 `S(i,t)` 只能使用 `A(i,t)` 當日或之前可公開取得的原始版本。之後發布的更正、重編、分類變更或資料庫回填值不得覆寫歷史訊號。

每次資料重跑須可選擇：

- **as-reported view（正式回測用）**：使用當時版本；
- **latest-restated view（診斷用）**：使用最新重編資料，僅供比較，不得報告為主結果。

禁止將目標期後才發布的資料混入特徵、缺失值補值模型、winsorization 邊界、產業平均或標準化參數。

---

## 4. 資料來源、優先級與資料品質

### 4.1 資料來源優先級

同一欄位衝突時依以下順序採用，並保留所有候選值與選擇理由：

1. 公司正式財務報告、合併財報、年報與法定申報附件；
2. 主管機關或交易所的原始申報／公告平台；
3. 公司投資人關係網站所發布且可驗證日期的文件；
4. 可信金融資料庫，僅作交叉核對或在原件不可得時的暫代來源；
5. 新聞、研究報告與第三方摘要僅供發現線索，不可作正式數值來源。

每個數值必須具有 `source_tier`、`source_url_or_path`、`source_document_id`、`extraction_method`（人工、表格擷取或 API）與 `retrieved_at`。

### 4.2 最低原始欄位

以合併報表、累計與單季原始值為必要時並存。下列欄位為 Phase 1 資料字典的最低集合：

| 類別 | 欄位 |
|---|---|
| 損益表 | revenue、cost_of_revenue/COGS、gross_profit、operating_expenses、operating_income、net_income、EPS、income_tax、non_operating_items |
| 現金流量表 | operating_cash_flow、capital_expenditure、free_cash_flow（若非公司直接揭露，保留計算公式）、stock_based_compensation（如適用） |
| 資產負債表 | cash_and_equivalents、total_debt、net_debt、accounts_receivable、inventory、deferred_revenue、shareholders_equity、shares_outstanding |
| 營運資料 | units_sold、ASP、產品／地區營收、產品組合、主要客戶集中度；僅在公司正式揭露時收錄 |
| 主檔與品質 | company_id、ticker、名稱、產業分類與有效日、會計期間、公告／可得日、幣別、合併口徑、來源、修訂版本、缺失原因、品質旗標 |

`free_cash_flow` 的正式預設定義為 `operating_cash_flow - capital_expenditure_cash_paid`。若 CapEx 的現金支付額不可辨認，不得以資產負債表增量替代；設為缺失並標記原因。

### 4.3 季度值處理

公司若僅揭露年初至今累計值，單季值以相鄰累計期間相減：

`Q(i,t) = YTD(i,t) - YTD(i,t-1)`。

此轉換須保存原始累計值、前一期來源及 `derived_from_ytd = true`。第一季累計值可視為第一季值。若因重編、會計年度變更、合併範圍改變或缺少可比前期而無法可信拆分，該季設為缺失。

### 4.4 年度資料（輔助頻率）

季度資料是所有 Phase 1 特徵、目標與主要統計檢驗的唯一主頻率。年度財報僅用於：

- 核對四個可比單季加總是否與年度正式報表一致；
- 提供三至五年趨勢、管理層資本配置與附註揭露的背景；
- 識別重編、會計政策、重大一次性項目、股份稀釋與合併範圍變動；
- 在季度現金流、CapEx 或產品資料不足時揭露「資料不足」，而不是把年度值拆配到季度。

年度資料的 `availability_date` 亦必須獨立保存。除非在未來版本另行預註冊，年度資料不得與季度資料混合成單一觀測值、不得用年度最終數字回填先前季度特徵，也不得取代季度主檢驗。

### 4.5 缺失、異常與不可比較資料

- 不得以零、產業平均、未來值或模型預測填補財務欄位。
- 分母為零、符號不具經濟意義、單位不明或期間不可比較時，相關比率為缺失，而非無限大或任意截尾值。
- 非經常性項目、資產減損、處分損益、稅率異常與會計估計／分類改變必須有旗標；本版不自行「正常化」數字。
- 缺失樣本不得靜默刪除。每一訊號—目標—期間報告必須列出：母體數、可用樣本數、各欄位缺失數、排除原因與失敗／下市樣本數。

---

## 5. 共通計算約定

令 `R(i,t)` 為單季營收、`C(i,t)` 為單季 COGS、`GP(i,t)=R-C`、`OI(i,t)` 為營業利益、`NI(i,t)` 為稅後淨利、`OCF(i,t)` 為營業現金流、`FCF(i,t)=OCF-CapEx`。

### 5.1 比率與成長率

除非特別說明，所有比率採單季值，所有同比成長採同一會計季度的前年同期值：

`YoY_X(i,t) = X(i,t) / X(i,t-4) - 1`

僅當 `X(i,t-4)` 非零且同口徑可比時定義。對可能為負的科目（如 OI、NI、FCF），不使用百分比成長率作主要訊號或主要目標；改用相對營收的變化或列為描述性分析，避免負基期造成誤導。

`GM(i,t) = GP(i,t) / R(i,t)`  
`OM(i,t) = OI(i,t) / R(i,t)`  
`OCF_M(i,t) = OCF(i,t) / R(i,t)`  
`FCF_M(i,t) = FCF(i,t) / R(i,t)`

所有百分比以小數儲存，呈現時才轉為百分比。不得在每一步先行四捨五入。

### 5.2 可比性與極端值

合併、分割、重大會計準則改變、主要業務出售／取得或公司明示不可比的季度，須標為 `comparability_flag`。主分析排除該事件直接造成的不可比成長率；保留原資料並在敏感度分析中另列。

極端值不在原始資料層改寫。若採 winsorization、rank 或 robust regression，邊界、群組、估計期與是否使用訓練資料必須在 analysis plan 預先固定，並只以當時可得的訓練／歷史資料估計。

---

## 6. 五大訊號：正式定義

每項訊號均以截至 `A(i,t)` 的資料計算。訊號值與資料品質旗標分開保存；「缺失」不是負向訊號。

### 6.1 S1：Revenue Growth Acceleration（營收成長加速）

`RG(i,t) = YoY_R(i,t)`  
`S1(i,t) = RG(i,t) - RG(i,t-1)`

含義為本季營收年增率相對前一季年增率的變化，單位為百分點（儲存為小數差）。正值代表同比成長加速，負值代表減速。

所需歷史為 `R(i,t)`, `R(i,t-1)`, `R(i,t-4)`, `R(i,t-5)`；任一不可比或缺失則 `S1` 缺失。此定義不把季節性環比誤當成成長。

### 6.2 S2：Price–Cost / Unit Economics（售價—成本／單位經濟）

S2 分為兩種**不得混為一談**的可用層級。

**Level A：公司層級成本—營收利差代理值（所有可計算公司）**

`CostRatio(i,t) = C(i,t) / R(i,t)`  
`S2A(i,t) = -[CostRatio(i,t) - CostRatio(i,t-4)]`

正值表示 COGS／營收同比下降，即每一元營收所需成本下降；負值表示成本壓力增加。`S2A` 是總體毛利結構代理值，**不是** ASP、單位成本或產品級毛利。

**Level B：產品／單位經濟（僅限有正式可比數量資料者）**

對產品 `p`，在公司正式揭露且營收與數量範圍一致時：

`ASP(i,p,t) = Revenue(i,p,t) / Units(i,p,t)`  
`EstimatedUnitCost(i,p,t) = AllocatedCOGS(i,p,t) / Units(i,p,t)`  
`EstimatedUnitGrossMargin(i,p,t) = [Revenue(i,p,t)-AllocatedCOGS(i,p,t)] / Revenue(i,p,t)`

只有公司直接揭露、且可與產品營收一一對應的 `AllocatedCOGS` 才可計算估計單位成本。不得以公司總 COGS 除單一產品銷量來冒充產品成本。

若有可比 ASP 與單位成本資料，產品層訊號定義為：

`S2B(i,p,t) = [ASP(i,p,t)/ASP(i,p,t-4)-1] - [EstimatedUnitCost(i,p,t)/EstimatedUnitCost(i,p,t-4)-1]`

各產品要彙總為公司值時，彙總公式與權重必須在資料取得完成、但看任何目標結果前預註冊；未預註冊前僅做產品層描述，不納入跨公司主檢驗。`S2B` 永遠附帶 `estimated = true` 與披露範圍說明。

### 6.3 S3：Gross / Operating Margin Expansion（毛利／營業利益率擴張）

`GM_Delta(i,t) = GM(i,t) - GM(i,t-4)`  
`OM_Delta(i,t) = OM(i,t) - OM(i,t-4)`

S3 是二元訊號組，不預設將毛利率與營業利益率任意加權成單一分數。主分析分別報告 `S3_GM = GM_Delta` 與 `S3_OM = OM_Delta`；任何合成指標的權重、方向與缺失處理，均須在 analysis plan 預註冊後才可使用。

### 6.4 S4：Earnings → Cash Conversion（獲利現金轉換）

單季現金流可能受營運資金與付款時點顯著波動，因此主訊號採最近四季滾動總和（TTM）：

`NI_TTM(i,t) = Σ(k=0..3) NI(i,t-k)`  
`OCF_TTM(i,t) = Σ(k=0..3) OCF(i,t-k)`  
`FCF_TTM(i,t) = Σ(k=0..3) FCF(i,t-k)`  
`S4_OCF(i,t) = OCF_TTM(i,t) / NI_TTM(i,t)`  
`S4_FCF(i,t) = FCF_TTM(i,t) / NI_TTM(i,t)`

僅當 `NI_TTM > 0` 且四季資料完整時定義；否則不以比率強行排序，記為缺失並另列為「TTM 淨利非正」狀態。主分析不把 OCF 或 FCF 為負的情況截斷為零。

輔助的營收尺度定義為 `OCF_TTM/R_TTM` 與 `FCF_TTM/R_TTM`，用於診斷非正淨利公司，但不與 S4 主比率混合排名。

### 6.5 S5：Fundamental Strength（基本面綜合強度）

S5 的目的，是在單一訊號完成驗證後才研究多面向共同改善；v0.1 **不定義任意權重或總分**。

本版僅建立可審計的 component panel：`S1`、`S2A`、`S3_GM`、`S3_OM`、`S4_OCF`、`S4_FCF`，以及可用時的 `S2B`。任何 S5 composite 須符合以下條件才可進入分析：

1. 僅使用在訓練期已固定的方向、標準化方法、納入成分、權重與缺失規則；
2. 不得使用 test 期目標、報酬、顯著性或排行榜選擇成分／權重；
3. 其規格以獨立的 `COMPOSITE_PRE_REGISTRATION` 版本記錄；
4. 必須與所有單一訊號結果並列，不得以 composite 掩蓋單一訊號失敗。

在上述條件未完成前，S5 是資料結構，不是可宣稱有效的預測分數。

---

## 7. Phase 1 目標（Future Fundamental Performance）

### 7.1 預測時點與 horizon

訊號於 `A(i,t)` 建立；目標使用未來已完成的財報季度。正式 horizon 集合為 `H = {1, 2, 4}` 季，分別表示 `t+1`、`t+2`、`t+4` 的單一未來季度。主要 horizon 及其餘 horizon 必須在開始正式檢驗前於 `ANALYSIS_PLAN.md` 指定；若未指定，三者一律同等報告，不得事後只挑最佳者。

### 7.2 目標字典

| 目標代碼 | 精確定義 | 經濟含義 |
|---|---|---|
| `Y_RG_h` | `YoY_R(i,t+h)` | 未來營收年增率 |
| `Y_GM_h` | `GM(i,t+h)` | 未來毛利率 |
| `Y_OM_h` | `OM(i,t+h)` | 未來營業利益率 |
| `Y_GM_delta_h` | `GM(i,t+h)-GM(i,t)` | 未來相對當期的毛利率改善 |
| `Y_OM_delta_h` | `OM(i,t+h)-OM(i,t)` | 未來相對當期的營業利益率改善 |
| `Y_OCF_margin_h` | `OCF_TTM(i,t+h)/R_TTM(i,t+h)` | 未來營業現金流率 |
| `Y_FCF_margin_h` | `FCF_TTM(i,t+h)/R_TTM(i,t+h)` | 未來自由現金流率 |
| `Y_OCF_conversion_h` | `OCF_TTM(i,t+h)/NI_TTM(i,t+h)`，僅 `NI_TTM>0` | 未來 OCF 現金轉換 |
| `Y_FCF_conversion_h` | `FCF_TTM(i,t+h)/NI_TTM(i,t+h)`，僅 `NI_TTM>0` | 未來 FCF 現金轉換 |

每個訊號對所有經濟相關目標進行檢驗，至少包括：S1→`Y_RG_h`；S2→`Y_GM_h`、`Y_GM_delta_h`；S3→`Y_GM_delta_h`、`Y_OM_delta_h`；S4→現金流率與現金轉換目標。其餘配對屬探索性，須標示為 exploratory。

不得使用股票報酬、價格、估值倍數或分析師預期作為 Phase 1 主要目標。這些僅屬 Phase 2 範圍，且 Phase 2 不得在 Phase 1 規格鎖定前啟動。

---

## 8. 產業標準化與比較規則

不同產業的毛利結構、營運週期與現金流模式差異大，因此不得將原始比率跨產業直接解讀為優劣。

### 8.1 分組

使用歷史有效的交易所／主管機關產業分類。主分組層級、最低群組樣本數與小群組處理方式必須在分析前預註冊。若某公司—季度所在產業群組樣本不足，該觀測值不產生產業標準化分數；不得併入「看起來相近」的產業。

### 8.2 標準化

主分析對每一 `as_of_date` 可得的同產業公司橫斷面，將訊號轉為產業內分位數／排名；精確排名法、同分處理、最小樣本數、是否以歷史滾動視窗計算與極端值處理，均在 analysis plan 鎖定。

原始值、產業調整值與未調整結果必須並存並報告。產業調整只用於比較，不得刪除經濟上「不漂亮」的產業。

---

## 9. 偏誤防制

### 9.1 Look-ahead bias

- 特徵只可用 `as_of_date` 前公開資料；目標只可在特徵後的未來期間取值。
- 特徵工程、產業統計量、缺失處理、縮尾邊界與模型轉換器必須在訓練資料 fit，再套用 validation/test。
- 不得使用資料庫的最新修正值取代歷史 as-reported 數據。
- 程式必須有自動檢查：任何來源資料的 `availability_date > as_of_date` 即中止該筆訊號生成。

### 9.2 Survivorship bias

Universe 必須由每個歷史時點可觀察的上市櫃公司名單建立，保留後續下市、破產、重整、併購與停止申報者。禁止使用當前存續公司清單回溯建樣本。

### 9.3 Data snooping 與多重檢驗

- 五大訊號、目標、horizon、產業規則、模型集合與成功標準先於 test 期結果固定。
- 不得因某一指標、產業、時間窗、資料清理方案或權重在 test 表現較佳而選擇性報告。
- 所有預先註冊的檢驗都報告；探索性檢驗要與主檢驗明確分離。
- 對同一檢驗族群的 p-value 採預先指定的多重比較校正；若尚未預註冊方法，主報告只提供未校正 p-value 並標示為描述性，不能宣稱顯著。

### 9.4 其他反偏誤檢查

主報告必須包含：存續者與退出者樣本比例、資料缺失模式、產業與年度覆蓋、極端值敏感度、單一產業／大型公司驅動情形。結論另須檢查 confirmation bias、recency bias、anchoring、narrative fallacy、overconfidence 與 base-rate neglect。

---

## 10. 訓練、驗證與測試

### 10.1 時間序切分

以**訊號的 `as_of_date`** 排序，而非財報期末日。資料先依序分成 train、validation、test 三段，且 test 必須是最後一段、完全未參與任何設計選擇。

具體日期切點、最短訓練長度、validation／test 長度與 embargo 長度，待資料覆蓋範圍確認後，在未檢視任何 test 結果前寫入 `ANALYSIS_PLAN.md`。切點一經鎖定即不可因效果調整。

### 10.2 Horizon embargo

對 horizon `h`，任何其目標財報尚未公開的樣本不得進入可用訓練／驗證結果。為防止相鄰切分的目標重疊，切分邊界至少設置涵蓋最大研究 horizon 的 embargo／purge 規則；精確作法須在 analysis plan 預先固定並於程式測試驗證。

### 10.3 模型複雜度

v0.1 主研究採可解釋的單訊號排序與預先指定回歸，不以高維機器學習最佳化為目標。若加入模型、控制變數或 composite，只能在 train 中選擇、validation 中確認，test 僅執行一次最終評估。

---

## 11. 統計檢驗與評估指標

### 11.1 必報描述統計

每個訊號—目標—horizon—樣本切分，報告觀測數、公司數、產業數、均值、中位數、標準差、分位數、缺失比例與退出公司比例。

### 11.2 主檢驗

對每一預註冊的訊號—目標配對，至少執行：

1. 產業內訊號分位組（分組數量預註冊）之未來目標平均／中位數，以及最高組減最低組差；
2. Spearman rank correlation（IC）與各期間 IC 的平均；
3. 預先指定的橫斷面／panel 回歸：`Y(i,t,h) = α + β S_norm(i,t) + industry_effect + time_effect + ε`。

標準誤的群聚方式、時間相關處理、異質變異處理與最小有效期數，必須於 analysis plan 固定。若資料結構不支持某種推論（例如有效期間不足），不得產生虛假的 p-value，改報描述性結果與限制。

### 11.3 樣本外評估

validation 用於確認規格可行性，test 僅評估鎖定規格。每個 test 結果至少報告：

- 方向是否與預註冊一致；
- top-minus-bottom 的效果量與信賴區間；
- IC、IC 的時間序平均與不確定性；
- 回歸係數、信賴區間、p-value（若推論假設成立）；
- 跨產業、跨年度與排除單一大型公司後的穩健性；
- 可用樣本與全部母體的差異。

不得把單一 p-value 當成經濟意義。效果量、跨期穩定性、樣本外重現性、資料品質與經濟可解釋性應共同判讀。

---

## 12. 成功、失敗與停止條件

本版不臆定「顯著」門檻、IC 下限、分位差下限或 composite 權重。這些若要成為決策門檻，必須在看 test 前預註冊，並記錄經濟與統計理由。

下列情況必須被明確報告為失敗、不可判斷或限制，而非事後重寫論點：

- 所需資料的來源、公告日或口徑無法驗證；
- 可用樣本量、有效時間期數或產業覆蓋不足以支持預註冊推論；
- 訊號方向在 validation/test 不穩定，或由單一產業、少數公司、單一期段驅動；
- as-reported 結果無法重現，只在 latest-restated 資料有效；
- 結果只在未預註冊的篩選、權重、horizon 或資料處理後出現；
- 權重／門檻需要看結果後才「有效」。

若任一主要訊號失敗，結論是該規格下未獲支持，不得改稱為負向或無效的普遍證明。應保留版本、原因與可預先規劃的下一步。

---

## 13. 可重現性與實作交付規格

Phase 1 Python 實作必須將原始資料、轉換資料、訊號、目標與報告分層保存，並禁止覆蓋歷史 as-reported 資料。

### 13.1 最低產物

- `data/raw/`：不可變原始檔或其可驗證快照；
- `data/processed/`：標準化欄位、資料品質旗標與轉換紀錄；
- `data/features_as_reported/`：逐公司—季度訊號與 `as_of_date`；
- `data/targets/`：未來目標，與特徵資料分開生成；
- `configs/`：資料來源對照、分類對照與已鎖定參數；
- `ANALYSIS_PLAN.md`：test 前鎖定的切分、統計選擇、多重校正與未定參數；
- `reports/`：含樣本流、結果、限制與版本資訊的輸出；
- `tests/`：公式、日期防漏、季度拆分、資料版本與切分邏輯測試。

若實際專案目錄不同，可調整路徑名稱，但上述資料責任邊界不得改變。

### 13.2 必要 metadata 與版本控制

每次產出必須記錄：本規格版本、程式 commit hash、Python 與套件鎖定版本、執行時間與時區、原始資料檔案雜湊、資料擷取時間、來源清單、設定檔雜湊、隨機種子（如適用）、樣本篩選紀錄與輸出檔雜湊。

原始資料不可被手動覆寫；清理與衍生欄位必須可由程式重新生成。任一手動判斷（例如分類、重編可比性）必須寫入版本控制的表格，包含操作者、日期、理由與證據連結。

### 13.3 程式不變量（必測）

1. 任何 feature 的來源 `availability_date` 不得晚於其 `as_of_date`；
2. 任何 target 期不得早於或等於 feature 期；
3. TTM 必須剛好由四個連續可比季度組成；
4. YTD 拆分只能使用同年度、同口徑的前期累計值；
5. 所有 ratio 的分母合法性必須顯式檢查；
6. 訓練資料的轉換參數不得由 validation/test fit；
7. 下市／失敗公司不得在歷史 Universe 建立時消失；
8. 任何缺失排除都必須出現在樣本流統計中。

---

## 14. 預註冊狀態與變更控制

以下事項在 v0.1 明確保留為未定參數：主要 horizon、樣本期切點、產業最小樣本數、排名／分組方法、winsorization、控制變數、標準誤與多重比較校正、S5 成分與權重、以及任何經濟／統計成功門檻。

這些參數必須在資料工程完成、但**尚未查看 test 結果**前，寫入版本控制的 `ANALYSIS_PLAN.md` 並標記為 locked。鎖定後：

- 不得因 validation 或 test 的結果改變主要定義；
- 有必要變更時，建立新版本與完整變更理由；原版本結果仍須保留；
- 新版本若參考過舊 test 結果，其檢驗一律視為探索性，必須使用新的、未接觸的時間外樣本才可成為確認性結果。

---

## 15. Phase 邊界與未來擴充

Phase 1 的完成條件不是「找到漂亮訊號」，而是交付可重跑、無時間穿越、完整披露失敗與限制的基本面研究結果。

只有在 Phase 1 的資料管線、訊號定義、結果與變更紀錄鎖定後，才可新增 Phase 2：`Fundamental Signal → Market Reaction`。Phase 2 必須另立規格，處理價格資料、交易成本、公告時點、可交易性、停牌、公司行動與市場基準；不得把這些問題倒灌改寫 Phase 1。

---

## 16. 本版本的明確禁止事項

- 不得以熱門題材、股價表現或新聞敘事取代財報資料；
- 不得把資料庫最新數字當作過去當時可得資料；
- 不得把 COGS／公司總營收代理值說成產品實際單位成本；
- 不得在未預註冊情況下臆定權重、閾值或成功標準；
- 不得看過結果後挑選產業、期間、horizon、模型或清理方式；
- 不得忽略下市、失敗、缺失與不可比較樣本；
- 不得只呈現有利結果；
- 不得把 Phase 1 的關聯性表述成因果或投資報酬保證。

本文件為 Phase 1 Python 實作的唯一研究規格來源；程式實作若與本文衝突，以本文為準，直到有版本化修訂正式取代。
